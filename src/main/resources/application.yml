logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE

---

spring:
  profiles:
    active: dev

---

spring:
  profiles:
    active: real

---

spring:
  profiles:
    active: test